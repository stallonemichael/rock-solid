import React from 'react';
import { withRouter } from 'react-router-dom';

import './projects.scss';

const ProjectsPage = () => <div className='projects'>Projects Page</div>;

export default withRouter(ProjectsPage);
